const express = require('express');
const router = express.Router();
// const apiUserController = require('./')

/* GET users listing. */
router.post('/auth');
router.post('/register');

module.exports = router;
