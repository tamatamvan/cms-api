const express = require('express');
const router = express.Router();

/* GET users listing. */
router.get('/', );
router.get('/search_date/:date');
router.get('/search_frequency/:freq');
router.post('/');
router.put('/:id', );
router.delete('/:id',);

module.exports = router;
