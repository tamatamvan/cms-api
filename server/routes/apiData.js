const express = require('express');
const router = express.Router();

/* GET users listing. */
router.get('/', );
router.get('/search_letter/:letter');
router.get('/search_frequency/:freq');
router.post('/');
router.put('/:id', );
router.delete('/:id',);

module.exports = router;
